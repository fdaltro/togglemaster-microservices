name: CI Flag Service

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # Job 1: Linter / Análise Estática
  linter:
    name: Linter & Static Analysis (Flag)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: aws-actions/setup-go@v5
        with:
          go-version: '1.21'
          cache-tracking-file: flag-service-main/go.sum

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v4
        with:
          version: latest
          working-directory: flag-service-main
          args: --timeout=5m

  # Job 2: Build & Unit Test
  build-test:
    name: Build & Unit Test (Flag)
    needs: linter
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
          cache-tracking-file: flag-service-main/go.sum

      - name: Install dependencies
        run: |
          cd flag-service-main
          go mod download

      - name: Run Unit Tests
        run: |
          cd flag-service-main
          go test -v ./...

      - name: Build Check
        run: |
          cd flag-service-main
          go build -v -o flag-service .