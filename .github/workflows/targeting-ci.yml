name: CI Targeting Service

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # Job 1: Linter / Análise Estática
  linter:
    name: Linter & Static Analysis (Targeting)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
          cache-tracking-file: targeting-service-main/go.sum

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v4
        with:
          version: latest
          working-directory: targeting-service-main
          args: --timeout=5m

  # Job 2: Build & Unit Test
  build-test:
    name: Build & Unit Test (Targeting)
    needs: linter
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
          cache-tracking-file: targeting-service-main/go.sum

      - name: Install dependencies
        run: |
          cd targeting-service-main
          go mod download

      - name: Run Unit Tests
        run: |
          cd targeting-service-main
          go test -v ./...

      - name: Build Check
        run: |
          cd targeting-service-main
          go build -v -o targeting-service .